apiVersion: v1
kind: Pod
metadata:
  name: example-no-conflict-with-limitrange-cpu
  namespace: nshelm
spec:
  containers:
  - name: demo
    image: nginx:1.25
    resources:
      requests:
        cpu: 700m
      limits:
        cpu: 1.2
